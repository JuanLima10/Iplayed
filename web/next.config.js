/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'https',
      'cdn.discordapp.com', 
      'images.igdb.com',
    ],
  },
}

module.exports = nextConfig
